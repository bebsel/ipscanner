# ipscanner
This is a simple Python application that functions as an ARP Network Scanner. It's designed to discover all active devices (hosts) on the local network by mapping their IP addresses and corresponding MAC addresses.
